import 'package:flutter/widgets.dart';

import 'assets.dart';

class AssetImages {
  AssetImages._();

  /// Assets for defaultPlaceholder
  /// default_placeholder
  static AssetImage get defaultPlaceholder =>
      AssetImage(Assets.defaultPlaceholder);

  /// Assets for readBg
  /// read_bg
  static AssetImage get readBg => AssetImage(Assets.readBg);
}
